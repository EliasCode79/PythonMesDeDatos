# To obtain the date in python we must import a python module 
from datetime import date

# date is an object and we can make use of its methods, now we will call the method today
is_today = date.today()
print(is_today)
